import { styled } from "./styles";

export const Button = styled("button", {
  color: "$white",
  fontFamily: "$default",
  backgroundColor: "$ignite700",
  border: 0,
  padding: "$2",
  borderRadius: "$md",
});
