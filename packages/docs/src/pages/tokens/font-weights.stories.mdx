import { Meta } from '@storybook/addon-docs'
import { TokenGrid } from '../../components/TokensGrid'
import { fontWeights } from '@ascendent_leonardo/tokens'

<Meta title="Tokens/font-weights" />

# font-Weights

These are the font-weights available on vortex UI

<TokenGrid tokens={fontWeights} />